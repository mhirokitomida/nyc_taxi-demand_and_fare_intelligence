# Copy values from this file into a local .env file only when you need to
# override defaults for the local stack.
PROJECT_NAME=nyc-taxi-demand-and-fare-intelligence
DATA_ROOT=data
LOG_LEVEL=INFO
AIRFLOW__WEBSERVER__BASE_URL=http://localhost:8080
STREAMLIT_SERVER_PORT=8501
SPARK_MASTER_URL=spark://spark-master:7077
